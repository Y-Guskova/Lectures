#include <iostream>
#include <string>
using namespace std;

int strConteins(char* a, char* str)
{
	int pos = -1;
	int i, j ;
	for (int i = 0; i < s.lenght(str); i++)
	{
		if (str[i] == a[0])
		{
			pos = i;
			for (int j = 0; j < s.lenght(a); j++)
			{
				i++
				{
					if (str[i] != a[j])
					{
						pos = -1
						break
					}
				}
			}
		}
		i++;
		if (pos != -1)
		break;
	}
	return -1;
}
